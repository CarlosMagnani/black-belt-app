For a GitHub issue labelled `ready-for-agent`, load the `implement` skill before making code changes. Work on exactly one ticket in an isolated worktree. Require its acceptance criteria and pre-agreed test seams. Delegate exploration and review before committing. Never merge, deploy, or change domain rules, authentication, payment processing, QR check-in, secrets, or destructive migrations.
